export default function ApiRootPage() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>Khepree API</h1>
      <p>
        Business API at <code>/api/v1/*</code> — api.khepree.com
      </p>
    </main>
  );
}
